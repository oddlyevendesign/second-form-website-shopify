{{ 'sf-corporate.css' | asset_url | stylesheet_tag }}

{%- assign _w = block.settings.width -%}
{%- if _w == 'custom' -%}{%- assign _w = block.settings.custom_width | append: '%' -%}{%- endif -%}
{%- assign _h = block.settings.height -%}
{%- if _h == 'custom' -%}{%- assign _h = block.settings.custom_height | append: 'px' -%}{%- endif -%}
<div class="corp-how-step" {{ block.shopify_attributes }} style="width:{{ _w }};height:{{ _h }};">
  <span class="corp-how-step__num"></span>
  {%- if block.settings.image -%}
    <div style="margin-bottom:16px;width:48px;height:48px;">
      <img src="{{ block.settings.image | image_url: width: 80 }}" alt="" width="48" height="48" style="object-fit:contain;">
    </div>
  {%- endif -%}
  <div class="corp-how-step__title">{{ block.settings.title }}</div>
  <div class="corp-how-step__body">{{ block.settings.body }}</div>
</div>

{% schema %}
{
  "name": "Corp: Step",
  "tag": null,
  "settings": [
    { "type": "image_picker", "id": "image", "label": "Step icon (optional)" },
    { "type": "richtext", "id": "title", "label": "Title", "default": "<p>Submit Inquiry</p>" },
    { "type": "richtext", "id": "body", "label": "Description", "default": "<p>Share your requirements, timeline, and budget through our proposal form.</p>" },
    { "type": "header", "content": "Layout" },
    {
      "type": "select", "id": "width", "label": "Width",
      "options": [
        { "value": "fit-content", "label": "Fit" },
        { "value": "100%", "label": "Fill" },
        { "value": "custom", "label": "Custom" }
      ],
      "default": "100%"
    },
    {
      "type": "range", "id": "custom_width", "label": "Custom width",
      "min": 10, "max": 100, "step": 5, "unit": "%", "default": 50,
      "visible_if": "{{ block.settings.width == 'custom' }}"
    },
    {
      "type": "select", "id": "height", "label": "Height",
      "options": [
        { "value": "fit-content", "label": "Fit" },
        { "value": "100%", "label": "Fill" },
        { "value": "custom", "label": "Custom" }
      ],
      "default": "fit-content"
    },
    {
      "type": "range", "id": "custom_height", "label": "Custom height",
      "min": 50, "max": 800, "step": 10, "unit": "px", "default": 200,
      "visible_if": "{{ block.settings.height == 'custom' }}"
    }
  ],
  "presets": [
    { "name": "Corp: Step", "category": "Corporate" }
  ]
}
{% endschema %}
